using System.Collections.Concurrent;
using HttpReactor.Client;

namespace HttpReactor
{
    public sealed class HttpReactor
    {
        //private readonly ConcurrentQueue<HttpClient> 

        public HttpClient GetClient()
        {
            return null;
        }
    }
}
